# text-viz — 自己完結 (jp_nlp_toolkit はリポジトリ内に同梱済み)
# Streamlit UI
streamlit>=1.32

# jp_nlp_toolkit コア
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
nltk>=3.8
networkx>=3.2
matplotlib>=3.7.0
plotly>=5.17.0
seaborn>=0.13.0
wordcloud>=1.9.0
nlplot>=1.6.0
tqdm>=4.66.0
pydantic>=2.5.0
typer>=0.9.0
pyyaml>=6.0

# 日本語スタック (fugashi / GiNZA / SudachiPy / oseti)
fugashi>=1.3.0
unidic-lite>=1.0.8
ja-ginza>=5.2.0
spacy>=3.7.0
sudachipy>=0.6.8
sudachidict-full>=20240109
jaconv>=0.3.4
neologdn>=0.5.3
japanize-matplotlib>=1.1.3
oseti>=0.4.3
# oseti は MeCab (mecab-python3) を使う。辞書が同梱されないため ipadic を追加。
ipadic>=1.0.0

# トピックモデル
gensim>=4.3.2
pyLDAvis>=3.4.0

# 埋め込み & 類似検索 (torch 含むため ~1GB)
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
# transformers を pin: oseti → bunkai が WordpieceTokenizer (4.40+ で削除) を使う一方、
# sentence-transformers は transformers >= 4.30 程度を要求する。両者の境目を指定。
transformers>=4.30,<4.40

# 共起ネットワーク (pyvis + Louvain)
pyvis>=0.3.2
python-louvain>=0.16

# 次元削減
umap-learn>=0.5.5

# 対応分析
prince>=0.13.0

# SOM
minisom>=2.3.1
